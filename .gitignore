# Node / wrangler
node_modules/
.wrangler/

# Stray JSON — soccer app backups and OneDrive auto-exports land at repo
# ROOT. Anchored to root only: src/data/*.json must stay trackable (see
# STATE.md backlog #8, balance-data-as-data).
/*.json
# Exception: root package.json if the project ever gains one
!package.json

# OS noise
.DS_Store
Thumbs.db

# Build output — generated by deploy_scc.ps1, never hand-edited
public/
